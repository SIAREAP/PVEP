PVEP_OPENAI_API_KEY=
# Required: record the exact provider or proxy endpoint for each new run.
PVEP_OPENAI_BASE_URL=
# Required: record the exact provider model identifier for each new run.
PVEP_OPENAI_MODEL=

# Optional aliases used by the thermal/sleeve LLM proposal runner.
SLEEVE_LLM_PROVIDER=
SLEEVE_LLM_API_KEY=
SLEEVE_LLM_BASE_URL=
SLEEVE_LLM_MODEL=
